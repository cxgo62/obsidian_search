## 概论
动态规划的动态的基本含义
1. 走一步看一步，把问题分解为n个阶段，比如01背包问题，==每个阶段只需要考虑当前阶段要不要放入对应的物品，不用考虑前序和后序的逻辑==
2. 本质上相当于进行==广度优先搜索==
3. 搜索可能导致分支爆炸，所以需要加入状态的cache来对每层的==节点进行合并==


## 一模型三特征
### 一模型
可不可以用一个转移方程来解决问题，也就是问题的各个步奏具有递归解决的可能

三特征
### 最优子结构
子问题的最优解
后面阶段的状态可以通过前面阶段的状态推导出来
### 无后效性
1. 在推导后面阶段的状态的时候，我们只关心前面阶段的状态值，不关心这个状态是怎么一步一步推导出来的
2. 某阶段状态一旦确定，就不受之后阶段的决策影响

### 重复子问题
也就是可以利用cache防止分支爆炸

## 01背包问题
给定一个物品序列和背包的最大容量，如何才能装入最大重量的物品组合
f(i, cw) 决策函数，表示在背包已有容量为w的情况下，考虑第i个物品是否装入 

状态的转移，第i层决策是否房屋index为i的物品
![[Pasted image 20211026115511.png|400]]
递归+cache的方式，将所有的可能放入cache中，最后检查cache中存在的最大w就可以

```java
private int maxW = Integer.MIN_VALUE; // 结果放到maxW中
private int[] weight = {2，2，4，6，3};  // 物品重量
private int n = 5; // 物品个数
private int w = 9; // 背包承受的最大重量
private boolean[][] mem = new boolean[5][10]; // 备忘录，默认值false


public void f(int i, int cw) { // 调用f(0, 0)
  if (cw == w || i == n) { // cw==w表示装满了，i==n表示物品都考察完了
    if (cw > maxW) maxW = cw;
    return;
  }
  if (mem[i][cw]) return; // 重复状态
  
  mem[i][cw] = true; // 记录(i, cw)这个状态
  f(i+1, cw); // 选择不装第i个物品
  if (cw + weight[i] <= w) {
    f(i+1,cw + weight[i]); // 选择装第i个物品
  }
}
```

非递归的实现方式，本质上和递归没有区别，基于循环实现的广度优先搜索

![[Pasted image 20211026120316.png|500]]
```java

weight:物品重量，n:物品个数，w:背包可承载重量
public int knapsack(int[] weight, int n, int w) {
  boolean[][] states = new boolean[n][w+1]; // 默认值false
  states[0][0] = true;  // 第一行的数据要特殊处理，可以利用哨兵优化
  if (weight[0] <= w) {
    states[0][weight[0]] = true;
  }
  for (int i = 1; i < n; ++i) { // 动态规划状态转移
    for (int j = 0; j <= w; ++j) {// 不把第i个物品放入背包
      if (states[i-1][j] == true) states[i][j] = states[i-1][j];
    }
    for (int j = 0; j <= w-weight[i]; ++j) {//把第i个物品放入背包
      if (states[i-1][j]==true) states[i][j+weight[i]] = true;
    }
  }
  for (int i = w; i >= 0; --i) { // 输出结果
    if (states[n-1][i] == true) return i;
  }
  return 0;
}
```

## 带物品价值的01背包
决策函数变为f(i,cw,cv) 在决策第i个物品是否放入时，会同时影响到重量和价值，cw和cv是在做决策前的初始值
状态转移变为
![[Pasted image 20211026121325.png|400]]

这里会加入==剪枝策略==
对于f(2,2,3)和f(2,2,4)两种情况来说，同样考入放入index为2的物品，且当背包中已有的重量都为2，那价值为3的这个节点就没必要继续下去

```java

public static int knapsack3(int[] weight, int[] value, int n, int w) {
  int[][] states = new int[n][w+1];
  for (int i = 0; i < n; ++i) { // 初始化states
    for (int j = 0; j < w+1; ++j) {
      states[i][j] = -1;
    }
  }
  states[0][0] = 0;
  if (weight[0] <= w) {
    states[0][weight[0]] = value[0];
  }
  for (int i = 1; i < n; ++i) { //动态规划，状态转移
    for (int j = 0; j <= w; ++j) { // 不选择第i个物品
      if (states[i-1][j] >= 0) states[i][j] = states[i-1][j];
    }
    for (int j = 0; j <= w-weight[i]; ++j) { // 选择第i个物品
      if (states[i-1][j] >= 0) {
        int v = states[i-1][j] + value[i];
        if (v > states[i][j+weight[i]]) {
          states[i][j+weight[i]] = v;
        }
      }
    }
  }
  // 找出最大值
  int maxvalue = -1;
  for (int j = 0; j <= w; ++j) {
    if (states[n-1][j] > maxvalue) maxvalue = states[n-1][j];
  }
  return maxvalue;
}
```