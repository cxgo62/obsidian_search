---
{}
---

## hash分区
对于key计算一个hash值，将hash值映射到一个逻辑分区，再将逻辑分区映射到物理分区上

好处：
1. 打的比较散，不容易出现热点
2. 实现比较容易，不容易出错

缺点：
1. 无法实现范围查询，范围查询要扫全表 [[30.2.10.1 hash分区的意义]]


## range分区
对数据范围进行分区，相邻的数据连续的放在一起，例如hbase

好处：
1. 方便进行范围查询

缺点：
1. 分区的大小会变化，需要持续的split和merge
2. 写入性能没有hash分区好，容易在某次写操作时触发split操作，导致突发的性能问题